server:
  port: 8484
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/micorservices_ratings
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
