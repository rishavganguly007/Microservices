server:
  port: 8181
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rishavganguly007/microservice-config
          clone-on-start: true