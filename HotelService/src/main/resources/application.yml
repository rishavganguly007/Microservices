server:
  port: 8383
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/micorservices?createDatabaseIfNotExist=true
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
